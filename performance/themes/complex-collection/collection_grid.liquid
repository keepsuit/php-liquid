<section class="products" data-count="{{ collection.products | size }}">
{% for product in collection.products %}  {% render "product_card", product: product, label: collection.label, position: forloop.index %}
{% endfor %}</section>
