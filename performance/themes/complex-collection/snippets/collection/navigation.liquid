<nav aria-label="Collection filters" class="collection-navigation">
  <p>{% render "snippets.shared.icon", name: "filter" %} Filter by</p>
  <ul>{% for tag in collection.tags %}
    <li data-position="{{ forloop.index }}">{{ tag | replace: " ", "-" | upcase }}</li>{% endfor %}
  </ul>
</nav>
