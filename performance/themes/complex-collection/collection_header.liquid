<header class="collection-header">
  <p class="eyebrow">{{ collection.label | upcase | append: " collection" }}</p>
  <h1>{{ collection.title }}</h1>
  <p>{{ collection.description }}</p>
  <p class="count">{{ collection.products | size }} products selected</p>
</header>
