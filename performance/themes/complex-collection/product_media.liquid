<figure class="product-card__media" data-image="{{ product.handle | append: "-image" }}">
  <img alt="{{ product.title | escape }}" src="/images/{{ product.handle }}.jpg">
  <figcaption>{{ product.title }} by {{ product.vendor }}</figcaption>
</figure>
